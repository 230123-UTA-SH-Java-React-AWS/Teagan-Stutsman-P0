pipeline {
    agent any

    stages {
        stage('Building and Creating .jar File') {
            steps {
                echo 'Building the .jar file'

                sh 'mvn -version'
            }
        }
    }
}