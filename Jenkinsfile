pipeline {
    agent any

    stages {
        stage('Building and Creating .jar File') {
            steps {
                echo 'Building the .jar file some change'

                sh 'mvn -version'
            }
        }
    }
}